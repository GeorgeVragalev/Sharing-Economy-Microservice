hazelcast:
  cluster-name: "dev"
  network:
    join:
      multicast:
        enabled: false
      tcp-ip:
        enabled: true
        member-list:
          - "hazelcast:5701"  # Assuming you're running a single instance